'use babel';

import LatexTextDecorationView from '../lib/latex-text-decoration-view';

describe('LatexTextDecorationView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
